import { join } from "path";

export const COMMANDS_DIR = join(__dirname, "../commands/");
export const CHIPS_PATH = join(__dirname, "../../db/chips.sqlite");
export const CDS_PATH = join(__dirname, "../../db/cds.sqlite");
